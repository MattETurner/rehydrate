[package]
name = "rehydrate-sync"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "reHydrate sync engine: orchestrates device <-> library transfers."

[dependencies]
rehydrate-core = { path = "../rehydrate-core" }
rehydrate-device = { path = "../rehydrate-device" }

futures = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true }
tracing = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }
tokio = { workspace = true, features = ["macros", "rt"] }
secrecy = { workspace = true }
# Tests reach for `FakeDevice` via the `fake` feature, plus `ssh` for
# the smoke test against a real tablet.
rehydrate-device = { path = "../rehydrate-device", features = ["ssh", "fake"] }
